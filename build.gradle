plugins {
    id 'java'
}

group 'org.example'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

jar{
    from {
        configurations.runtimeClasspath.collect { it.isDirectory() ? it : zipTree(it) }
    }
}

//test{
//    useJUnitPlatform()
//}

dependencies {
    compile group: 'org.apache.lucene', name: 'lucene-queries', version: '8.4.1'
    compile group: 'org.apache.lucene', name: 'lucene-core', version: '8.4.1'
    compile group: 'org.apache.lucene', name: 'lucene-sandbox', version: '8.4.1'
}
